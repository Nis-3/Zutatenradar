package de.jakueche.jannis.zutatenradar.controller;

// TODO: REST-Controller fuer Kategorien (@RestController, @RequestMapping("/api/categories"))
//
// Endpoints:
//   GET /api/categories       -> alle Kategorien auflisten
//   GET /api/categories/{id}  -> einzelne Kategorie mit zugehoerigen Rezepten
//
// Braucht kein vollstaendiges CRUD -- Kategorien werden ueber DataSeeder angelegt.
// Kann spaeter erweitert werden.

public class CategoryController {
}
